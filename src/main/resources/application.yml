spring:
  application:
    name: jogging
  datasource:
    driverClassName: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://127.0.0.1/jlweb
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
server:
  port: 9000
webber:
  app:
    jwtSecret: Zm9vYmFy
    jwtExpirationMinutes: 180
